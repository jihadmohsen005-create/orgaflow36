# ⚡ دليل البدء السريع - OrgaFlow36

## 🎯 الهدف

تشغيل نظام OrgaFlow36 مع قاعدة بيانات Supabase في أقل من 10 دقائق!

---

## ✅ المتطلبات

- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn
- حساب Supabase (مجاني)
- متصفح حديث

---

## 🚀 الخطوات

### 1️⃣ تحميل المشروع

```bash
# إذا كان لديك المشروع بالفعل
cd orgaflow36

# تثبيت المكتبات
npm install
```

---

### 2️⃣ إعداد Supabase

#### أ. إنشاء مشروع جديد

1. اذهب إلى: https://app.supabase.com
2. انقر **"New Project"**
3. املأ البيانات:
   - Name: `OrgaFlow36`
   - Password: اختر كلمة مرور قوية
   - Region: `Frankfurt` (أو الأقرب لك)
4. انقر **"Create new project"**
5. انتظر 1-2 دقيقة

#### ب. تشغيل ملفات SQL

1. في Supabase Dashboard، اذهب إلى **SQL Editor**
2. انقر **"New query"**
3. انسخ محتوى `supabase/schema.sql` والصقه
4. انقر **"Run"** (أو Ctrl+Enter)
5. كرر نفس الخطوات لـ:
   - `supabase/schema_part2.sql`
   - `supabase/schema_part3.sql`
   - `supabase/seed_data.sql`

#### ج. الحصول على المفاتيح

1. اذهب إلى **Settings** → **API**
2. انسخ:
   - **Project URL** (مثل: `https://xxxxx.supabase.co`)
   - **anon public** key

---

### 3️⃣ إعداد ملف البيئة

1. أنشئ ملف `.env.local` في المجلد الرئيسي:

```bash
# في Windows PowerShell
New-Item .env.local

# في Mac/Linux
touch .env.local
```

2. افتح الملف وأضف:

```env
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

3. استبدل القيم بالمفاتيح التي نسختها

---

### 4️⃣ تشغيل التطبيق

```bash
npm run dev
```

سيفتح التطبيق على: http://localhost:5173

---

### 5️⃣ تسجيل الدخول

استخدم أحد الحسابات التجريبية:

| المستخدم | كلمة المرور | الدور |
|---------|-------------|-------|
| `director` | `123` | مدير تنفيذي |
| `finance` | `123` | مدير مالي |
| `procurement` | `123` | موظف مشتريات |
| `accountant` | `123` | محاسب |
| `auditor` | `123` | مدقق |

---

## ✨ تهانينا!

الآن لديك نظام OrgaFlow36 يعمل بكامل طاقته مع قاعدة بيانات حقيقية! 🎉

---

## 🔍 التحقق من البيانات

### في Supabase Dashboard:

1. اذهب إلى **Table Editor**
2. تحقق من الجداول:
   - `users` → يجب أن تجد 5 مستخدمين
   - `suppliers` → يجب أن تجد 3 موردين
   - `items` → يجب أن تجد 3 أصناف
   - `projects` → قد تكون فارغة (يمكنك إضافة مشاريع من التطبيق)

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "Failed to fetch"

**الحل:**
- تحقق من اتصال الإنترنت
- تحقق من `VITE_SUPABASE_URL` في `.env.local`

---

### المشكلة: "Invalid API key"

**الحل:**
- تحقق من `VITE_SUPABASE_ANON_KEY` في `.env.local`
- تأكد من نسخ المفتاح الصحيح من Supabase

---

### المشكلة: "relation does not exist"

**الحل:**
- تأكد من تشغيل جميع ملفات SQL بالترتيب
- في SQL Editor، شغّل:
  ```sql
  SELECT table_name FROM information_schema.tables 
  WHERE table_schema = 'public';
  ```
- يجب أن تظهر جميع الجداول

---

### المشكلة: الصفحة فارغة

**الحل:**
1. افتح Console (اضغط F12)
2. تحقق من الأخطاء
3. تأكد من تشغيل `seed_data.sql`

---

## 📖 الخطوات التالية

1. **اقرأ الوثائق:**
   - `SUPABASE_INTEGRATION.md` - دليل الدمج الكامل
   - `DEVELOPER_GUIDE.md` - دليل المطورين
   - `supabase/README.md` - دليل قاعدة البيانات

2. **جرّب الميزات:**
   - أضف مورد جديد
   - أنشئ طلب شراء
   - أضف مشروع جديد

3. **استكشف الكود:**
   - `src/services/` - طبقة الخدمات
   - `src/hooks/` - Custom Hooks
   - `src/pages/` - صفحات التطبيق

---

## 🆘 تحتاج مساعدة؟

- راجع `DEVELOPER_GUIDE.md` للتفاصيل التقنية
- تحقق من Supabase Logs في Dashboard
- افتح Console في المتصفح (F12) للأخطاء

---

## 🎓 نصائح للمبتدئين

1. **ابدأ بالقراءة فقط:**
   - جرّب عرض البيانات أولاً
   - لا تقلق بشأن الإضافة والتعديل في البداية

2. **استخدم Supabase Dashboard:**
   - يمكنك إضافة/تعديل البيانات مباشرة من Dashboard
   - مفيد للتجربة والتعلم

3. **تابع Console:**
   - اضغط F12 دائماً لمتابعة ما يحدث
   - ستتعلم الكثير من رسائل الأخطاء

---

**استمتع بالتطوير! 🚀**

