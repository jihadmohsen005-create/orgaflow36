# 📋 الصفحات التي تحتاج تحديث لاستخدام Supabase

## ✅ **الصفحات المحدثة (تستخدم Supabase)**

1. ✅ **PoliciesAndManualsPage.tsx** - تم التحديث
2. ✅ **CorrespondencePage.tsx** - تم التحديث

---

## ⚠️ **الصفحات التي تحتاج تحديث (تحفظ في State فقط)**

### **1. DepartmentsPage.tsx**
- **الجدول:** `departments`
- **المشكلة:** يحفظ في State فقط (السطر 71, 77, 96)
- **الحل المطلوب:**
  - إضافة `useEffect` لجلب البيانات من Supabase
  - تحديث `handleAction` لاستخدام `supabase.from('departments').insert/update/delete`

### **2. DonorsPage.tsx**
- **الجدول:** `donors`
- **المشكلة:** يحفظ في State فقط (السطر 81, 87, 105)
- **الحل المطلوب:**
  - إضافة `useEffect` لجلب البيانات
  - تحديث `handleAction` لاستخدام Supabase

### **3. BoardMembersPage.tsx** (MasterBoardMember)
- **الجدول:** `master_board_members`
- **المشكلة:** يحفظ في State فقط (السطر 82, 88, 105)
- **الحل المطلوب:**
  - إضافة `useEffect` لجلب البيانات
  - تحديث `handleAction` لاستخدام Supabase

### **4. BankAccountsPage.tsx**
- **الجداول:** `banks`, `bank_sub_accounts`
- **المشكلة:** يحفظ في State فقط (السطر 75, 81, 102-114)
- **الحل المطلوب:**
  - إضافة `useEffect` لجلب البيانات
  - تحديث `handleSaveBank` و `handleSaveSubAccount` لاستخدام Supabase

### **5. BoardOfDirectorsPage.tsx** (BoardSession)
- **الجدول:** `board_sessions`, `board_members`
- **المشكلة:** يحفظ في State فقط
- **الحل المطلوب:**
  - إضافة `useEffect` لجلب البيانات
  - تحديث `handleAction` لاستخدام Supabase

---

## 📊 **الصفحات التي تستخدم Supabase بالفعل**

هذه الصفحات تستخدم خدمات Supabase من خلال `src/services/`:

1. ✅ **SuppliersPage.tsx** - يستخدم `supplierService.ts`
2. ✅ **ItemsPage.tsx** - يستخدم `itemService.ts`
3. ✅ **PurchaseOrdersPage.tsx** - يستخدم `purchaseOrderService.ts`
4. ✅ **ContractsPage.tsx** - يستخدم `contractService.ts`
5. ✅ **ProjectsPage.tsx** - يستخدم `projectService.ts`
6. ✅ **UsersPage.tsx** - يستخدم `userService.ts`
7. ✅ **EmployeesPage.tsx** - يستخدم `employeeService.ts`

---

## 🎯 **الأولوية**

### **أولوية عالية (High Priority):**
1. **DepartmentsPage** - جدول مهم ومستخدم في أماكن كثيرة
2. **DonorsPage** - جدول مهم للمشاريع والتمويل

### **أولوية متوسطة (Medium Priority):**
3. **BoardMembersPage** - جدول مهم لمجلس الإدارة
4. **BankAccountsPage** - جدول مهم للمالية

### **أولوية منخفضة (Low Priority):**
5. **BoardOfDirectorsPage** - يمكن تأجيله

---

## 📝 **ملاحظات**

1. **جميع الصفحات المذكورة أعلاه** تحتاج نفس النمط من التحديث:
   - إضافة imports: `import { supabase } from '../supabaseClient';`
   - إضافة `useEffect` لجلب البيانات عند التحميل
   - تحديث دوال الحفظ/التحديث/الحذف لاستخدام Supabase

2. **لا توجد مرفقات** في هذه الصفحات، لذلك لا حاجة لـ `storageService`

3. **البيانات الحالية** في State ستضيع عند تحديث الصفحة، لذلك يجب:
   - إما تحديث الصفحات الآن
   - أو إضافة بيانات seed مباشرة في قاعدة البيانات

---

## 🚀 **الخطوة التالية**

**الأفضل الآن:** إضافة بيانات seed مباشرة في قاعدة البيانات بدلاً من تحديث جميع الصفحات.

**السبب:**
- توفير الوقت
- البيانات ستكون متاحة فوراً
- يمكن تحديث الصفحات لاحقاً عند الحاجة

**البيانات المطلوبة:**
1. ✅ `policy_manuals` - 3 سجلات مع مرفقات
2. ✅ `correspondence` - 3 سجلات مع مرفقات
3. ⏳ `departments` - 5 أقسام
4. ⏳ `donors` - 3 جهات مانحة
5. ⏳ `master_board_members` - 5 أعضاء
6. ⏳ `banks` - 2 بنوك
7. ⏳ `board_sessions` - 1 دورة

---

**هل تريد المتابعة مع إضافة البيانات التجريبية؟** 🎯

