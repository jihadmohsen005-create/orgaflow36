# 🚀 دليل النشر على Netlify - OrgaFlow36

## 📌 نظرة عامة

هذا الدليل سيساعدك على نشر تطبيق OrgaFlow36 على **Netlify** خطوة بخطوة.

**المدة المتوقعة:** 10-15 دقيقة

---

## ✅ المتطلبات الأساسية

تأكد من أن لديك:
- ✅ Git مثبت ومُعد
- ✅ حساب GitHub (أو ستنشئ واحد)
- ✅ المشروع جاهز محلياً

---

## 📋 الجزء الأول: رفع المشروع إلى GitHub

### الخطوة 1: تهيئة Git (إذا لم تكن قد فعلت)

افتح **Command Prompt** في مجلد المشروع:

```cmd
cd C:\Users\pc\Downloads\orgaflow36
```

ثم نفّذ:

```bash
# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# إنشاء أول commit
git commit -m "Initial commit - OrgaFlow36 ready for Netlify deployment"
```

---

### الخطوة 2: إنشاء Repository على GitHub

1. **افتح المتصفح** واذهب إلى:
   - إذا لم يكن لديك حساب: https://github.com/signup
   - إذا كان لديك حساب: https://github.com/login

2. **أنشئ Repository جديد:**
   - اذهب إلى: https://github.com/new
   - أو اضغط **"+"** في الأعلى → **"New repository"**

3. **املأ البيانات:**
   ```
   Repository name: orgaflow36
   Description: نظام إدارة شامل للمنظمات غير الربحية - OrgaFlow36
   
   اختر: ⚫ Private (خاص - موصى به للأمان)
          أو
          ⚪ Public (عام - إذا أردت مشاركة الكود)
   
   لا تضع علامة على:
   ❌ Add a README file
   ❌ Add .gitignore
   ❌ Choose a license
   ```

4. **اضغط** "Create repository" (الزر الأخضر في الأسفل)

---

### الخطوة 3: ربط المشروع بـ GitHub

بعد إنشاء Repository، ستظهر صفحة بها تعليمات. **انسخ اسم المستخدم** من الرابط:

```
https://github.com/YOUR_USERNAME/orgaflow36.git
```

في **Command Prompt**، نفّذ:

```bash
# استبدل YOUR_USERNAME باسم المستخدم الخاص بك
git remote add origin https://github.com/YOUR_USERNAME/orgaflow36.git

# تسمية الفرع الرئيسي
git branch -M main

# رفع الكود إلى GitHub
git push -u origin main
```

**ملاحظة مهمة - المصادقة:**

عند تنفيذ `git push`، سيطلب منك GitHub:
- **Username:** اسم المستخدم في GitHub
- **Password:** **لا تستخدم كلمة المرور العادية!**

**استخدم Personal Access Token:**

1. اذهب إلى: https://github.com/settings/tokens
2. اضغط **"Generate new token"** → **"Generate new token (classic)"**
3. في **"Note"**: اكتب `OrgaFlow36 Netlify Deployment`
4. في **"Expiration"**: اختر `90 days` أو `No expiration`
5. في **"Select scopes"**: ضع علامة ✓ على:
   - ✅ `repo` (كامل)
6. اضغط **"Generate token"** (الزر الأخضر في الأسفل)
7. **انسخ Token** (سيظهر مرة واحدة فقط! احفظه في مكان آمن)
8. الصق Token كـ **Password** في Command Prompt

**النتيجة المتوقعة:**
```
Enumerating objects: 150, done.
Counting objects: 100% (150/150), done.
...
To https://github.com/YOUR_USERNAME/orgaflow36.git
 * [new branch]      main -> main
```

---

## 🌐 الجزء الثاني: النشر على Netlify

### الخطوة 4: إنشاء حساب Netlify

1. **افتح المتصفح** واذهب إلى: https://app.netlify.com/signup

2. **اختر طريقة التسجيل:**
   - **موصى به:** اضغط **"GitHub"** (للربط المباشر)
   - أو استخدم البريد الإلكتروني

3. **إذا اخترت GitHub:**
   - سجّل دخول بحساب GitHub
   - اضغط **"Authorize Netlify"**
   - امنح Netlify الصلاحيات المطلوبة

4. **أكمل إعداد الحساب:**
   - قد يطلب منك اسم الفريق (Team name)
   - اكتب: `orgaflow` أو أي اسم تريده
   - اضغط **"Continue"**

---

### الخطوة 5: استيراد المشروع من GitHub

1. **في Netlify Dashboard:**
   - اضغط **"Add new site"** (زر في الأعلى)
   - اختر **"Import an existing project"**

2. **اختر مصدر Git:**
   - اضغط **"Deploy with GitHub"**
   - إذا طُلب منك، امنح Netlify صلاحيات إضافية

3. **اختر Repository:**
   - ابحث عن `orgaflow36`
   - اضغط عليه لاختياره

4. **إذا لم يظهر Repository:**
   - اضغط **"Configure Netlify on GitHub"**
   - اختر حسابك
   - في **"Repository access"**:
     - اختر **"Only select repositories"**
     - اختر `orgaflow36`
   - اضغط **"Save"**
   - ارجع إلى Netlify وحدّث الصفحة

---

### الخطوة 6: تكوين إعدادات البناء

في صفحة **"Site settings for orgaflow36"**:

#### **1. Basic build settings:**

```
Branch to deploy: main
```

#### **2. Build settings:**

Netlify سيكتشف Vite تلقائياً، لكن تأكد من:

```
Base directory: (اتركه فارغاً)

Build command: npm run build

Publish directory: dist

Functions directory: (اتركه فارغاً)
```

#### **3. Advanced build settings:**

اضغط **"Show advanced"** ثم **"New variable"**

---

### الخطوة 7: إضافة متغيرات البيئة (مهم جداً!)

أضف المتغيرات التالية واحدة تلو الأخرى:

#### **المتغير الأول:**
```
Key: VITE_SUPABASE_URL
Value: https://dihtjatqgwyyuvilacdd.supabase.co
```
اضغط **"Add variable"** أو **"+"**

#### **المتغير الثاني:**
```
Key: VITE_SUPABASE_ANON_KEY
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRpaHRqYXRxZ3d5eXV2aWxhY2RkIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQwMTc0MDMsImV4cCI6MjA3OTU5MzQwM30.Ck7mKYM5g44SF4S3JkIjqpOWx6u582R9KCxqGLaIJ9A
```
اضغط **"Add variable"** أو **"+"**

#### **المتغير الثالث:**
```
Key: GEMINI_API_KEY
Value: PLACEHOLDER_API_KEY
```
اضغط **"Add variable"** أو **"+"**

**تحقق من أن جميع المتغيرات الثلاثة ظهرت في القائمة!**

---

### الخطوة 8: بدء النشر

1. **اضغط** "Deploy orgaflow36" (الزر الكبير في الأسفل)

2. **انتظر عملية البناء:**
   - ستظهر صفحة **"Production deploys"**
   - سترى حالة البناء: **"Building"** 🔨
   - المدة المتوقعة: **2-4 دقائق**

3. **راقب سجل البناء (Build log):**
   - اضغط على النشر الجاري (الصف الأول)
   - اضغط **"Deploy log"** لرؤية التفاصيل
   - يجب أن ترى:
     ```
     npm run build
     vite build
     ✓ built in XXXms
     ```

4. **عند اكتمال البناء:**
   - ستتغير الحالة إلى: **"Published"** ✅
   - سيظهر رابط الموقع

---

## 🎉 الجزء الثالث: الوصول إلى التطبيق

### الخطوة 9: فتح التطبيق

1. **انسخ رابط الموقع:**
   - سيكون بالشكل:
   ```
   https://random-name-123456.netlify.app
   ```

2. **افتح الرابط في المتصفح**

3. **جرّب تسجيل الدخول:**
   ```
   Username: director
   Password: 123
   ```

---

### الخطوة 10: تخصيص اسم الموقع (اختياري)

إذا أردت تغيير الاسم العشوائي:

1. في Netlify Dashboard → **"Site settings"**
2. اذهب إلى **"Site information"** → **"Change site name"**
3. اكتب الاسم الجديد (مثلاً: `orgaflow36`)
4. اضغط **"Save"**

**الرابط الجديد سيكون:**
```
https://orgaflow36.netlify.app
```

---

## ✅ الجزء الرابع: التحقق والاختبار

### قائمة الاختبار:

- [ ] **الصفحة الرئيسية تظهر بشكل صحيح**
- [ ] **اللغة العربية تعمل (RTL)**
- [ ] **تسجيل الدخول يعمل**
- [ ] **قاعدة البيانات متصلة (البيانات تظهر)**
- [ ] **التنقل بين الصفحات يعمل**
- [ ] **رفع الملفات يعمل (جرّب في "الصادر والوارد")**
- [ ] **لا توجد أخطاء في Console (اضغط F12)**

---

## 🔄 الجزء الخامس: التحديثات المستقبلية

### كيف تنشر تحديث جديد؟

**بسيط جداً!** كل مرة تريد نشر تحديث:

```bash
# 1. احفظ التغييرات
git add .

# 2. أنشئ commit
git commit -m "وصف التحديث"

# 3. ارفع إلى GitHub
git push
```

**Netlify سينشر التحديث تلقائياً خلال 2-3 دقائق!** 🎉

---

## 🎯 ملخص الخطوات

1. ✅ رفع المشروع إلى GitHub
2. ✅ إنشاء حساب Netlify
3. ✅ استيراد المشروع من GitHub
4. ✅ تكوين إعدادات البناء
5. ✅ إضافة متغيرات البيئة
6. ✅ النشر
7. ✅ الاختبار
8. ✅ تخصيص الاسم (اختياري)

---

## 📊 مقارنة: Netlify vs Vercel

| الميزة | Netlify | Vercel |
|--------|---------|--------|
| السعر | مجاني | مجاني |
| سهولة الاستخدام | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| سرعة البناء | ⚡ سريع | ⚡ سريع جداً |
| CDN | ✅ عالمي | ✅ عالمي |
| Forms | ✅ مدمج | ❌ |
| Functions | ✅ نعم | ✅ نعم |
| Analytics | ✅ نعم | ✅ نعم |

**كلاهما ممتاز!** اخترت Netlify وهو خيار رائع! 🎉

---

## 🆘 حل المشاكل الشائعة

### مشكلة: Build failed

**الحل:**
1. افتح Deploy log
2. ابحث عن رسالة الخطأ
3. تأكد من أن `npm run build` يعمل محلياً
4. تحقق من متغيرات البيئة

### مشكلة: صفحة بيضاء

**الحل:**
1. افتح Console (F12)
2. تحقق من متغيرات البيئة في Netlify
3. تأكد من أن `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY` صحيحة

### مشكلة: 404 عند التنقل

**الحل:**
- تأكد من وجود ملف `netlify.toml` في المشروع
- تحقق من أن `redirects` مُعد بشكل صحيح

---

## 📞 روابط مفيدة

- **Netlify Dashboard:** https://app.netlify.com
- **Netlify Docs:** https://docs.netlify.com
- **Supabase Dashboard:** https://app.supabase.com
- **GitHub:** https://github.com

---

**تهانينا! 🎉 تطبيقك الآن على الإنترنت عبر Netlify!**

