# دليل إعداد قاعدة بيانات Supabase لنظام OrgaFlow36

## 📋 نظرة عامة

هذا الدليل يشرح كيفية إعداد قاعدة بيانات Supabase لنظام OrgaFlow36 خطوة بخطوة.

---

## 🚀 الخطوات

### 1. إنشاء مشروع Supabase جديد

1. اذهب إلى [Supabase Dashboard](https://app.supabase.com)
2. قم بتسجيل الدخول أو إنشاء حساب جديد
3. انقر على **"New Project"**
4. املأ البيانات التالية:
   - **Name**: OrgaFlow36
   - **Database Password**: اختر كلمة مرور قوية (احفظها!)
   - **Region**: اختر المنطقة الأقرب لك (مثل: Frankfurt لأوروبا والشرق الأوسط)
   - **Pricing Plan**: Free (للتجربة) أو Pro (للإنتاج)
5. انقر على **"Create new project"**
6. انتظر حتى يتم إنشاء المشروع (قد يستغرق دقيقة أو دقيقتين)

---

### 2. تشغيل ملفات SQL

بعد إنشاء المشروع، قم بتشغيل ملفات SQL بالترتيب التالي:

#### أ. الانتقال إلى SQL Editor

1. في لوحة تحكم Supabase، انقر على **"SQL Editor"** من القائمة الجانبية
2. انقر على **"New query"**

#### ب. تشغيل الملفات بالترتيب

**الملف 1: schema.sql**
```sql
-- انسخ محتوى ملف supabase/schema.sql والصقه في SQL Editor
-- ثم انقر على "Run" أو اضغط Ctrl+Enter
```

**الملف 2: schema_part2.sql**
```sql
-- انسخ محتوى ملف supabase/schema_part2.sql والصقه في SQL Editor
-- ثم انقر على "Run"
```

**الملف 3: schema_part3.sql**
```sql
-- انسخ محتوى ملف supabase/schema_part3.sql والصقه في SQL Editor
-- ثم انقر على "Run"
```

**الملف 4: seed_data.sql** (البيانات الأولية)
```sql
-- انسخ محتوى ملف supabase/seed_data.sql والصقه في SQL Editor
-- ثم انقر على "Run"
```

⚠️ **ملاحظة مهمة**: يجب تشغيل الملفات بالترتيب المذكور أعلاه!

---

### 3. الحصول على مفاتيح API

1. في لوحة تحكم Supabase، انقر على **"Settings"** (الإعدادات) من القائمة الجانبية
2. انقر على **"API"**
3. ستجد المعلومات التالية:

   - **Project URL**: انسخ الرابط (مثل: `https://xxxxx.supabase.co`)
   - **anon public**: انسخ المفتاح (هذا هو `SUPABASE_ANON_KEY`)

---

### 4. إعداد ملف البيئة (.env)

1. في مجلد المشروع الرئيسي، أنشئ ملف جديد باسم `.env.local`
2. أضف المعلومات التالية:

```env
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

3. استبدل القيم بالمفاتيح التي حصلت عليها من الخطوة السابقة

⚠️ **تحذير**: لا تشارك هذه المفاتيح مع أحد ولا ترفعها على GitHub!

---

### 5. تثبيت المكتبات المطلوبة

في terminal، قم بتشغيل:

```bash
npm install
```

هذا سيقوم بتثبيت جميع المكتبات المطلوبة بما في ذلك:
- `@supabase/supabase-js`
- `@tanstack/react-query`

---

### 6. تشغيل التطبيق

```bash
npm run dev
```

---

## 🔐 الأمان

### تفعيل Row Level Security (RLS)

لحماية البيانات، يُنصح بتفعيل Row Level Security على الجداول:

```sql
-- مثال: تفعيل RLS على جدول users
ALTER TABLE users ENABLE ROW LEVEL SECURITY;

-- إنشاء سياسة للسماح بقراءة البيانات للمستخدمين المصرح لهم
CREATE POLICY "Users can view their own data"
ON users FOR SELECT
USING (auth.uid() = id);
```

⚠️ **ملاحظة**: حالياً النظام يستخدم مصادقة بسيطة. في الإنتاج، يجب استخدام Supabase Auth.

---

## 📊 التحقق من البيانات

بعد تشغيل ملفات SQL، يمكنك التحقق من البيانات:

1. انقر على **"Table Editor"** في لوحة تحكم Supabase
2. ستجد جميع الجداول التي تم إنشاؤها
3. انقر على أي جدول لعرض البيانات

يجب أن تجد:
- ✅ 9 أدوار (Roles)
- ✅ 5 مستخدمين (Users)
- ✅ 3 موردين (Suppliers)
- ✅ 3 أصناف (Items)
- ✅ 2 جهات مانحة (Donors)
- ✅ وغيرها من البيانات الأولية

---

## 🔧 استكشاف الأخطاء

### خطأ: "relation does not exist"
- **السبب**: لم يتم تشغيل ملفات SQL بالترتيب الصحيح
- **الحل**: احذف جميع الجداول وأعد تشغيل الملفات بالترتيب

### خطأ: "duplicate key value"
- **السبب**: محاولة إدخال بيانات مكررة
- **الحل**: احذف البيانات الموجودة أو غيّر القيم في seed_data.sql

### خطأ: "Invalid API key"
- **السبب**: مفاتيح API غير صحيحة في ملف .env.local
- **الحل**: تحقق من المفاتيح في Supabase Dashboard

---

## 📚 موارد إضافية

- [Supabase Documentation](https://supabase.com/docs)
- [React Query Documentation](https://tanstack.com/query/latest)
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)

---

## 🆘 الدعم

إذا واجهت أي مشاكل:
1. تحقق من console في المتصفح (F12)
2. تحقق من Logs في Supabase Dashboard
3. راجع هذا الدليل مرة أخرى

---

**تم إنشاء هذا الدليل لنظام OrgaFlow36** 🚀

