# 🔧 دليل الإعداد الكامل - OrgaFlow36 مع Supabase

## 📋 الخطوة 1: الحصول على مفاتيح Supabase

### 1.1 الدخول إلى مشروعك

1. اذهب إلى: **https://app.supabase.com**
2. سجّل الدخول إلى حسابك
3. ستجد مشروع **OrgaFlow36** في قائمة المشاريع
4. **انقر على المشروع** لفتحه

---

### 1.2 الحصول على المفاتيح

بعد فتح المشروع:

1. في القائمة الجانبية اليسرى، انقر على أيقونة **⚙️ Settings** (الإعدادات)
2. من القائمة الفرعية، اختر **API**
3. ستجد قسمين مهمين:

#### أ. Project URL
```
https://xxxxxxxxxxxxx.supabase.co
```
- هذا هو رابط مشروعك
- **انسخه بالكامل**

#### ب. Project API keys
ستجد عدة مفاتيح، المطلوب هو:

**anon public** (المفتاح العام)
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS...
```
- هذا مفتاح طويل جداً (عدة أسطر)
- **انقر على أيقونة النسخ** 📋 بجانبه
- **لا تنسخ** المفتاح `service_role` (هذا سري ولا يُستخدم في Frontend)

---

### 1.3 إضافة المفاتيح إلى `.env.local`

1. افتح ملف `.env.local` في المشروع
2. استبدل القيم كالتالي:

```env
# Supabase Configuration
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS...

# Gemini API (Optional)
GEMINI_API_KEY=PLACEHOLDER_API_KEY
```

3. **احفظ الملف** (Ctrl+S)

---

## 🗄️ الخطوة 2: تشغيل ملفات SQL

### 2.1 فتح SQL Editor

1. في لوحة تحكم Supabase (نفس المشروع OrgaFlow36)
2. في القائمة الجانبية اليسرى، انقر على **🔧 SQL Editor**
3. ستفتح صفحة محرر SQL

---

### 2.2 تشغيل الملفات بالترتيب

**⚠️ مهم جداً: يجب تشغيل الملفات بهذا الترتيب بالضبط!**

#### الملف 1️⃣: `schema.sql` (الجداول الأساسية)

1. انقر على **+ New query** (استعلام جديد)
2. افتح ملف `supabase/schema.sql` من مشروعك
3. **انسخ المحتوى بالكامل** (Ctrl+A ثم Ctrl+C)
4. **الصق** في محرر SQL في Supabase
5. انقر على **▶️ Run** (أو اضغط Ctrl+Enter)
6. **انتظر** حتى يظهر:
   ```
   Success. No rows returned
   ```
7. إذا ظهرت أخطاء، **لا تكمل** - أخبرني بالخطأ

---

#### الملف 2️⃣: `schema_part2.sql` (العقود والموارد البشرية)

1. انقر على **+ New query** مرة أخرى
2. افتح ملف `supabase/schema_part2.sql`
3. **انسخ المحتوى بالكامل**
4. **الصق** في محرر SQL الجديد
5. انقر على **▶️ Run**
6. تحقق من رسالة النجاح

---

#### الملف 3️⃣: `schema_part3.sql` (العمليات والأصول)

1. انقر على **+ New query**
2. افتح ملف `supabase/schema_part3.sql`
3. **انسخ المحتوى بالكامل**
4. **الصق** في محرر SQL
5. انقر على **▶️ Run**
6. تحقق من رسالة النجاح

---

#### الملف 4️⃣: `seed_data.sql` (البيانات الأولية)

1. انقر على **+ New query**
2. افتح ملف `supabase/seed_data.sql`
3. **انسخ المحتوى بالكامل**
4. **الصق** في محرر SQL
5. انقر على **▶️ Run**
6. يجب أن تظهر رسالة تشير إلى إدراج البيانات بنجاح

---

### 2.3 التحقق من إنشاء الجداول

1. في القائمة الجانبية، انقر على **📊 Table Editor**
2. يجب أن تشاهد قائمة طويلة من الجداول، مثل:
   - `users`
   - `roles`
   - `suppliers`
   - `items`
   - `projects`
   - `purchase_requests`
   - وغيرها... (60+ جدول)

3. **انقر على جدول `users`**
4. يجب أن تشاهد **5 مستخدمين**:
   - director
   - finance
   - procurement
   - accountant
   - auditor

5. **انقر على جدول `suppliers`**
6. يجب أن تشاهد **3 موردين**

إذا رأيت البيانات، **تهانينا! ✅** قاعدة البيانات جاهزة!

---

## 🚀 الخطوة 3: تشغيل التطبيق

### 3.1 التأكد من تثبيت المكتبات

في Terminal (في مجلد المشروع):

```bash
npm install
```

---

### 3.2 تشغيل التطبيق

```bash
npm run dev
```

يجب أن تظهر رسالة مثل:

```
  VITE v6.2.0  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

---

### 3.3 فتح التطبيق

1. افتح المتصفح
2. اذهب إلى: **http://localhost:5173**
3. يجب أن تظهر صفحة تسجيل الدخول

---

## 🔐 الخطوة 4: تسجيل الدخول

استخدم أحد الحسابات التجريبية:

| اسم المستخدم | كلمة المرور | الدور |
|--------------|-------------|-------|
| `director` | `123` | مدير تنفيذي |
| `finance` | `123` | مدير مالي |
| `procurement` | `123` | موظف مشتريات |
| `accountant` | `123` | محاسب |
| `auditor` | `123` | مدقق |

---

## ✅ الخطوة 5: التحقق من الاتصال

بعد تسجيل الدخول:

1. افتح **Console** في المتصفح (اضغط F12)
2. اذهب إلى تبويب **Console**
3. **لا يجب** أن تشاهد أخطاء باللون الأحمر
4. جرّب فتح صفحة **الموردين** (Suppliers)
5. يجب أن تشاهد الموردين الثلاثة من قاعدة البيانات

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: "Failed to fetch"

**السبب**: خطأ في `VITE_SUPABASE_URL`

**الحل**:
1. تحقق من `.env.local`
2. تأكد أن الرابط صحيح وينتهي بـ `.supabase.co`
3. **أعد تشغيل** التطبيق (Ctrl+C ثم `npm run dev`)

---

### المشكلة 2: "Invalid API key"

**السبب**: خطأ في `VITE_SUPABASE_ANON_KEY`

**الحل**:
1. تحقق من `.env.local`
2. تأكد أنك نسخت المفتاح `anon public` وليس `service_role`
3. تأكد أن المفتاح كامل (طويل جداً)
4. **أعد تشغيل** التطبيق

---

### المشكلة 3: "relation does not exist"

**السبب**: لم يتم تشغيل ملفات SQL بشكل صحيح

**الحل**:
1. في Supabase SQL Editor، شغّل:
   ```sql
   SELECT table_name 
   FROM information_schema.tables 
   WHERE table_schema = 'public'
   ORDER BY table_name;
   ```
2. يجب أن تظهر 60+ جدول
3. إذا لم تظهر، أعد تشغيل ملفات SQL بالترتيب

---

### المشكلة 4: الصفحة فارغة / بيضاء

**السبب**: خطأ في JavaScript

**الحل**:
1. افتح Console (F12)
2. اقرأ رسالة الخطأ
3. أخبرني بالخطأ المحدد

---

## 📞 تحتاج مساعدة؟

إذا واجهت أي مشكلة:

1. **افتح Console** (F12) وانسخ رسالة الخطأ
2. **التقط صورة** للشاشة إذا لزم الأمر
3. **أخبرني** بالخطوة التي فشلت فيها

---

**حظاً موفقاً! 🚀**

